# Скопируйте этот файл в .env и подставьте свои значения.
# Файл .env уже в .gitignore — он не попадёт в репозиторий.

# Подключение к боевой БД проекта (PostgresPro)
DATABASE_URL=postgresql://fomin_id:fomin_id@245e1-rw.db.pub.dbaas.postgrespro.ru:5432/dbproject?sslmode=require

# Если хотите запустить с локальной SQLite — закомментируйте строку выше
# и раскомментируйте эту:
# DATABASE_URL=sqlite:///local.db
